# ITS 프로젝트 작업 로그

## 09-03 완료한 것

| 항목 | 내용 |
|---|---|
| 이벤트 건수 vs 고유 차량 수 구분 | `batch_compare.py`에 "고유 차량 수" 집계 추가 — 반복 감지로 인한 과다 카운트와 실제 이상 차량 수를 구분 |
| 급정거 오탐 대폭 개선 | 저속 구간 노이즈(60%→80% 감속 비율, 최소속도 20→25km/h)로 오탐 반복 완화 |
| 비현실적 속도 필터링 | 200km/h대 등 물리적으로 불가능한 속도(추적 오류)를 과속 판정에서 제외 (`max_plausible_speed`) |
| `meters_per_pixel` 보정 | 명령줄 옵션(`--meters-per-pixel`)으로 카메라 축척 조정 가능하게, 0.05→0.025로 재보정 |
| `batch_compare.py` 버그 수정 | 정지 이미지 소스까지 "영상"으로 잡히던 문제 해결 (`.mp4` 확장자 필터링) |
| **번호판 검출 전용 모델 학습** | Kaggle 데이터셋의 `number_plate` 라벨(272장/485박스)로 YOLOv8n 파인튜닝 → `models/plate_detector.pt` |
| 번호판 검출 파이프라인 | `inspect_plate_classes.py`(클래스 확인) → `build_plate_dataset.py`(데이터셋 구성) → `train_plate_detector.py`(학습) → `main.py` 자동 연결 |
| OCR 디버그 도구 | `--save-ocr-debug` 옵션으로 실제 크롭 이미지를 `debug_output/`에 저장, 인식 실패 원인을 눈으로 확인 가능하게 |
| DB ERD 문서화 | pgAdmin ERD 캡처를 README에 추가 |
| README 재구조화 | 상단 "한눈에 보기" 요약 + 하단 상세 섹션으로 구조화 |
| 이상탐지된 차량 번호판 자동 인식 연결 | `main.py`가 이상탐지 시 자동으로 번호판 인식 시도, `ocr_attempts` 통계로 집계 |

### 알게 된 한계 (정직하게 기록)
- **번호판 인식 실전 성공률: 0%.** 원인 확인 결과, 코드/모델 문제가 아니라 **촬영 각도 문제**임을 디버그 이미지로 직접 확인함 — 지금 쓰는 CCTV 영상은 차량을 측면에서 찍고 있어, 번호판이 애초에 화면에 안 보이는 각도임.
- 이는 알고리즘의 한계가 아니라 **입력 데이터(영상 촬영 각도)의 한계**로, 정면/후면 각도로 찍힌 CCTV라면 결과가 다를 것으로 예상됨.
- 개선하려면: (1) 정면/후면 각도의 CCTV 영상 확보, 또는 (2) 여러 각도 영상을 조합해 번호판이 보이는 프레임만 선별하는 로직 추가가 필요함 (향후 과제로 백로그에 등록).

---

## 09-02 완료한 것

| 항목 | 내용 |
|---|---|
| DB 구조 리팩터링 | `track_id`를 진짜 외래키(FK)로 연결 — `Track` 원본 테이블 신설, `detection_records`/`flow_features`/`anomaly_records`가 이를 참조 |
| 학습형 이상탐지 준비 | Isolation Forest 모듈(`ml_anomaly.py`), 학습 스크립트(`train_isolation_forest.py`) 추가 |
| 3단계: 차량 추적 로직 | IOU 전역 최적 매칭 + 클래스 일치 검증으로 고도화 (ID 스위칭 방지), 합성 데이터로 단위테스트 작성·검증(PASS) |
| 4단계: 파이프라인 연결 | `main.py`가 Track 생성 → flow_features 실시간 적재 → 이상 시 anomaly_records 기록까지 새 DB 구조에 맞게 완성 (영상 입력만 있으면 실행 가능) |
| 5단계: 이상탐지 규칙 검증 | 과속·역주행·급정거·불법정차 4개 규칙을 합성 데이터로 단위테스트 — 7/7 케이스 PASS |
| 8단계: 대시보드·API | `api.py`를 새 DB 구조에 맞게 수정, `/detections` `/stats` 엔드포인트 추가, 대시보드에 차종별 통계·검출결과 실시간 반영 확인 |
| 검출 정확도 개선 | YOLOv8n → YOLOv8s로 모델 교체 (신뢰도 낮은 문제 대응) |
| UI 개선 | 대시보드 시간 표시를 `YYYY-MM-DD HH:MM:SS` 형식으로 정리 |
| 기획 문서 | PRD를 RICE 스코어링·검증계획 포함한 실무형으로 재작성, 공식 통계(도로교통공단)로 배경 데이터 보강 |
| 4단계 실전 검증 | 실제 영상(`1.mp4`)으로 전체 파이프라인(검출→추적→속도계산→이상탐지→DB→대시보드) 첫 실행 성공 |
| 버그: 역주행 오탐 | 정상 방향을 고정값(1,0)으로 가정하던 문제 → 영상 앞부분을 훑어 방향을 자동 캘리브레이션, 양방향 도로는 2개 방향 모두 정상으로 인식하도록 개선 |
| 버그: 속도 노이즈 | 2프레임만으로 속도 계산해 프레임마다 급격히 튀던 문제 → 5프레임 평균으로 스무딩, 단 급정거 판단은 순간속도로 별도 계산해 민감도 유지 |
| 버그: 시간 표시 오류 | DB에 UTC로 저장되어 대시보드 시간이 실제보다 9시간 늦게 보이던 문제 → 로컬시간(KST) 저장으로 수정 |
| 버그: `/records` API 500 에러 | DB 세션 종료 후 관계(Track) 데이터에 접근하려다 발생한 `DetachedInstanceError` → `joinedload`로 세션이 열려있을 때 미리 로드하도록 수정 |
| 기능 추가 | `--speed-limit` 옵션으로 과속 임계값을 실행 시 조정 가능하게 (이상탐지 로직 검증 용도) |
| 형상관리 | 오늘 변경사항 전체 + 테스트 영상(`1.mp4`) GitHub push 완료 |
| 번호판 인식률 통계 | `ocr_attempts` 테이블 신설, 시도마다 성공/실패 기록, 대시보드에 성공률(%) 표시 |
| 여러 영상 비교 도구 | `batch_compare.py` — 영상 여러 개를 일괄 처리하고 차량수·평균속도·이상유형별 건수를 표로 비교 |
| 대시보드 실시간성 체감 개선 | "마지막 갱신 시각" + 깜빡이는 표시등 추가로 자동갱신(GET)이 실제로 동작 중임을 시각화 |
| POST 기능 실제 구현 | `/demo/anomaly`(버튼으로 즉시 DB 저장 확인), `/upload`(영상 업로드 시 백그라운드로 실제 파이프라인 실행) |
| 대시보드 차트화 | Chart.js 도입 — 차종별 막대, 이상유형별 막대, 번호판 인식률 도넛 3종 |
| 간이 속도 예측 모델 | `flow_features` 23,841건으로 RandomForest 학습 — 최근 3프레임 속도로 다음 속도 예측 (MAE 5.56km/h), 급정거 조기경고용 `predict_sudden_drop_risk()` 구현 |

---

## 09-01 완료한 것

| 항목 | 내용 |
|---|---|
| 개발 환경 | Python 3.11 가상환경(`its`) 구축, requirements.txt 설치 |
| 1단계: 데이터 수집 | Kaggle `traffic-vehicles-object-detection` 다운로드, `data/raw`로 정리 (이미지 823장) |
| 2단계: 차량 검출 | YOLOv8(`detect.py`) 동작 확인 — 이미지 1장에서 차량 3대 검출 성공 |
| DB 구축 | SQLite → PostgreSQL(`its` DB) 전환 |
| DB 데이터 적재 | 전체 이미지 검출 실행 → 실제 검출 결과 5,712건 저장 (`populate_db.py`) |
| 번호판 로직 | 한국 번호판 형식(숫자2~3+한글1+숫자4) 정규식 검증 추가 (`ocr.py`) |
| 형상관리 | 로컬 git 저장소 → GitHub 새 저장소(`mussinv6-commits/its-anomaly-detection`) 생성 및 push |

---

## 다음 할 것

| 순서 | 작업 | 파일 |
|---|---|---|
| 1 | 정면/후면 각도로 찍힌 CCTV 영상 확보 후 번호판 인식 재검증 | 새 영상 필요 |
| 2 | 예측 모델을 main.py 파이프라인에 실제로 연결 (실시간 조기경고) | `src/predict.py` |
| 3 | `train_isolation_forest.py`도 실제 flow_features로 재학습 | 6단계 |

---

## 다음에 추가할 것 (백로그)

| 순서 | 작업 | 비고 |
|---|---|---|
| 6단계 | 학습형 이상탐지 고도화 (Isolation Forest 실데이터 학습) | 4단계 실전 검증 완료로 이제 진행 가능 |
| 7단계 | 번호판 인식 각도 문제 해결 | 측면 촬영 영상에서는 구조적으로 불가능함을 확인, 정면/후면 영상 필요 |
| 9단계 | 테스트 및 발표자료 정리 | 검출 통계, 정확도 수치 포함 |
| 부가 | requirements.txt 버전 고정 재정비 | 지금은 버전 미고정 상태, 안정화 필요 |
| 부가 | Alembic 마이그레이션 도구 도입 | DB 구조 변경 이력 관리 |
| 부가 | 검출 신뢰도 추가 개선 검토 (yolov8m 또는 자체 데이터 파인튜닝) | 지금 yolov8s로 1차 개선함 |
| 부가 | 항만 화물차 도메인 추가 검토 | 과적 측정, 지정 경로 이탈 감지 (기초 단계: 공개 데이터/체크포인트 시뮬레이션) |

